apiVersion: v1
kind: Secret
metadata:
  name: jubileeverse-secrets
  namespace: jubileeverse
  labels:
    app.kubernetes.io/name: jubileeverse
type: Opaque
stringData:
  # IMPORTANT: Replace these with actual secrets in production
  # Use external secret management (Vault, AWS Secrets Manager, etc.)
  SESSION_SECRET: "change-this-in-production-use-strong-random-key"
  DB_USER: "jubilee"
  DB_PASSWORD: "change-this-password"
  DB_HOST: "postgres-service"
  REDIS_HOST: "redis-service"
  REDIS_PASSWORD: ""
  QDRANT_HOST: "qdrant-service"
  QDRANT_API_KEY: ""
  OPENAI_API_KEY: ""
  ANTHROPIC_API_KEY: ""
